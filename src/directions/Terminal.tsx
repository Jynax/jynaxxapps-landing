export default function Terminal() {
  return (
    <section
      data-direction="terminal"
      style={{ minHeight: '100%', padding: 40, background: 'var(--term-bg)', color: 'var(--term-fg)' }}
    >
      terminal — placeholder (Task 5)
    </section>
  )
}
