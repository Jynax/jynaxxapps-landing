export default function Console() {
  return (
    <section
      data-direction="console"
      style={{ minHeight: '100%', padding: 40, background: 'var(--con-bg)', color: 'var(--con-ink)' }}
    >
      console — placeholder (Task 6)
    </section>
  )
}
